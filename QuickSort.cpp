#include "QuickSort.h"


std::vector<int> Quicksort::sort(std::vector<int> list) {

int pivotIndex = list.at(0);

if(list.size() < 3) {
    int pivot = list.at(list.size()-1);
    }
else {
    int pivot = list.at(2);
}

for



}

